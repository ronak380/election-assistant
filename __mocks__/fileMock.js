/**
 * @file __mocks__/fileMock.js
 * @description Provides a mock string for static file imports (images, SVGs, etc.)
 *              so that Jest doesn't fail when components import asset files.
 */
module.exports = 'test-file-stub';
